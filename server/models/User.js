const mongoose = require('mongoose'); const schema = new mongoose.Schema({ name:String, email:{type:String,unique:true}, password:String, isAdmin:{type:Boolean,default:false}, blocked:{type:Boolean,default:false} }); module.exports = mongoose.model('User', schema);