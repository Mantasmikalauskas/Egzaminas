import React,{useState,useContext} from 'react'; import Header from '../components/Header'; import { AuthContext } from '../context/AuthContext'; export default function ApieMus(){ const [text,setText]=useState('Mūsų įmonė teikia nuomos paslaugas...'); const {user}=useContext(AuthContext); const [edit,setEdit]=useState(false); const save=()=>{ setEdit(false); alert('Išsaugota (demo)'); }; return (<div><Header /><div className='content'><main className='main'><h2>Apie mus</h2>{edit? <textarea value={text} onChange={e=>setText(e.target.value)} style={{width:'100%',height:200}}/> : <p>{text}</p>}{user?.role==='admin' && (edit? <button onClick={save} className='btn'>Išsaugoti</button> : <button onClick={()=>setEdit(true)} className='btn'>Koreguoti</button>)}</main></div><footer className='footer'>Kontaktai</footer></div>); }