import Car from '../models/Car.js'; export const listCars = async (req,res)=>{ const q = {}; if(req.query.class) q.carClass = req.query.class; const cars = await Car.find(q).sort({createdAt:-1}); res.json(cars); }; export const getCar = async (req,res)=>{ const car = await Car.findById(req.params.id); if(!car) return res.status(404).json({message:'Not found'}); res.json(car); }; export const createCar = async (req,res)=>{ const car = await Car.create(req.body); res.status(201).json(car); }; export const updateCar = async (req,res)=>{ const car = await Car.findByIdAndUpdate(req.params.id, req.body, {new:true}); if(!car) return res.status(404).json({message:'Not found'}); res.json(car); }; export const deleteCar = async (req,res)=>{ await Car.findByIdAndDelete(req.params.id); res.json({ok:true}); };