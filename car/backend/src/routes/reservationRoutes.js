import express from 'express'; import { createReservation, updateReservation, cancelReservation, getUserReservations, getAllReservations, changeReservationStatus } from '../controllers/reservationController.js'; import { protect, adminOnly } from '../middleware/authMiddleware.js'; const r = express.Router(); r.post('/', protect, createReservation); r.put('/:id', protect, updateReservation); r.delete('/:id', protect, cancelReservation); r.get('/me', protect, getUserReservations); r.get('/', protect, adminOnly, getAllReservations); r.patch('/:id/status', protect, adminOnly, changeReservationStatus); export default r;
