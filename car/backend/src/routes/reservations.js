import express from 'express'; import { createReservation, getMy, getAll, changeStatus } from '../controllers/reservationController.js'; import { protect, adminOnly } from '../middleware/auth.js'; const r = express.Router(); r.post('/', protect, createReservation); r.get('/me', protect, getMy); r.get('/', protect, adminOnly, getAll); r.patch('/:id/status', protect, adminOnly, changeStatus); export default r;
