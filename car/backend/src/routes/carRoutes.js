import express from 'express'; import { listCars,getCar,createCar,updateCar,changeStatus } from '../controllers/carController.js'; import { protect, adminOnly } from '../middleware/authMiddleware.js'; const r = express.Router(); r.get('/', listCars); r.get('/:id', getCar); r.post('/', protect, adminOnly, createCar); r.put('/:id', protect, adminOnly, updateCar); r.patch('/:id/status', protect, adminOnly, changeStatus); export default r;
