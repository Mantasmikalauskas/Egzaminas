import React,{useEffect,useState} from 'react'; import API from '../api'; import Header from '../components/Header';
export default function Reservations(){ const [items,setItems]=useState([]); useEffect(()=>{ API.get('/reservations/me').then(r=>setItems(r.data)).catch(()=>{}); },[]); return (<div><Header /><div className='content'><div className='main'><h2>Mano rezervacijos</h2><ul>{items.map(i=> (<li key={i._id}>{i.car?.make} {i.car?.model} - {new Date(i.startDate).toLocaleDateString()} to {new Date(i.endDate).toLocaleDateString()} | {i.status}</li>))}</ul></div></div><footer className='footer'>Kontaktai</footer></div>); }