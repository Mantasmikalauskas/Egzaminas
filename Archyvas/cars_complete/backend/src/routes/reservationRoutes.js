import express from 'express'; import { createReservation,getMy,getAll,updateReservation,deleteReservation } from '../controllers/reservationController.js'; import { protect, adminOnly } from '../middleware/authMiddleware.js'; const r=express.Router(); r.post('/',protect,createReservation); r.get('/me',protect,getMy); r.get('/',protect,adminOnly,getAll); r.put('/:id',protect,updateReservation); r.delete('/:id',protect,deleteReservation); export default r;